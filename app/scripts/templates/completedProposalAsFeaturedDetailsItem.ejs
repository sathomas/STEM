<div class="featured__details__image">
    <img src="<%= thumbImageURL %>" width="100" height="100"/>
</div>
<div class="featured__details__text">
    <h4 class="featured__details__title"><%= title %></h4>
    <p><%= shortDescription %></p>
    <p>
        <a href="<%= proposalURL %>" class="button button-link">
            $<% print(Math.round(+totalPrice)) %> funded
        </a>
    </p>
</div>
